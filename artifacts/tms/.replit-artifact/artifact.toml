kind = "web"
previewPath = "/"
title = "Transport Management System"
version = "1.0.0"
id = "artifacts/tms"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22625

[services.development]
run = "pnpm --filter @workspace/tms run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tms", "run", "build" ]
publicDir = "artifacts/tms/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22625"
BASE_PATH = "/"
